name: CI Build

on:
  push:
    branches: ["**"]
  pull_request:
    branches: ["**"]

jobs:
  build:
    name: Build on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, ubuntu-24.04, ubuntu-20.04, windows-latest]
        include:
          - os: ubuntu-latest
            preset: linux-release
          - os: ubuntu-24.04
            preset: linux-release
          - os: ubuntu-20.04
            preset: linux-release
          - os: windows-latest
            preset: windows-release

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up vcpkg
        uses: lukka/run-vcpkg@v11
        with:
          vcpkgGitCommitId: "b472291f295551b7127359ea38fdd2ca092f6f1b"

      - name: Configure & Build with CMake
        uses: lukka/run-cmake@v10
        with:
          cmakeListsTxtPath: "${{ github.workspace }}/CMakeLists.txt"
          configurePreset: ${{ matrix.preset }}
          buildPreset: ${{ matrix.preset }}

      - name: Package (CPack)
        shell: bash
        run: |
          cd "${{ github.workspace }}/build/${{ matrix.preset }}"
          cpack

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ci-test-${{ matrix.os }}
          path: |
            ${{ github.workspace }}/build/${{ matrix.preset }}/ci-test-*.tar.gz
            ${{ github.workspace }}/build/${{ matrix.preset }}/ci-test-*.zip
          if-no-files-found: error
